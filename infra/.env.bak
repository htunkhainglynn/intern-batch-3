# Change this before starting anything — this protects Redis once it's
# reachable over bore.pub.
REDIS_PASSWORD=changeMeStrongPassword

# This gets overwritten automatically by start-tunnels.sh with the live
# bore.pub:PORT for Kafka. You don't need to edit it by hand.
KAFKA_EXTERNAL_ADVERTISED_ADDR=bore.pub:23671
